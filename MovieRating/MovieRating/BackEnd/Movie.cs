using System;
using System.Collections.Generic;
using System.Configuration;
using System.Data.SqlClient;
using System.Linq;
using System.Web;

namespace MovieRating.BackEnd
{
    public class Movie
    {
         //string connStr = ConfigurationManager.ConnectionStrings["ConnStringDb1"].ConnectionString;
         //   SqlConnection conn = new SqlConnection(connStr)


        //static SqlConnection conn = new SqlConnection(connStr);

        //static string sql = "SELECT * from Movie";

        //static SqlCommand cmd = new SqlCommand(sql, conn);

        //static SqlDataReader reader = cmd.ExecuteReader();

        //private var value = "test";
    }
}